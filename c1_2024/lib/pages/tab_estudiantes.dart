import 'package:flutter/material.dart';

class TabEstudiantes extends StatelessWidget {
  const TabEstudiantes({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Página de Estudiantes'),
    );
  }
}
