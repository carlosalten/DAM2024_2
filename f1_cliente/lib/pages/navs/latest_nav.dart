import 'package:flutter/material.dart';

class LatestNav extends StatelessWidget {
  const LatestNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Latest'));
  }
}
