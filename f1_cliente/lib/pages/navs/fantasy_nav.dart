import 'package:flutter/material.dart';

class FantasyNav extends StatelessWidget {
  const FantasyNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Fantasy'));
  }
}
