import 'package:flutter/material.dart';

class RacingNav extends StatelessWidget {
  const RacingNav({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Racing'));
  }
}
